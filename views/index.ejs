<link rel="stylesheet" type="text/css" href="master.css">

<h1>Aperçu</h1>
<p>Expand your mind, 10 things at a time.</p>
<hr>
<ul>
  <h3>Featured Courses:</h3>
  <hr>
  <% for(var i=0; i < courses.length; i++) { %>
    <% if (courses[i].fields.featuredStatus == true ) { %>
      <li class="featured">
        <p><b>
          <a href="/courses/<%= courses[i].fields.slug  %>">
            <%= courses[i].fields.title  %>
          </a>
        </b></p>
        <p>by <%= courses[i].fields.curator.fields.name  %></p>
        <p><%= courses[i].fields.timeToComplete  %> hours</p>
        <!-- Really flexible image API here https://www.contentful.com/developers/docs/references/images-api/#/reference/resizing-&-cropping -->
        <img src="http:<%= courses[i].fields.thumbnail.fields.file.url  %>?w=300"/>
      </li>
    <% } %>
  <% } %>
</ul>
<ul>
  <h3>All Courses:</h3>
  <hr>
  <% for(var i=0; i < courses.length; i++) { %>
    <% if (courses[i].fields.featuredStatus !== true ) { %>
      <li>
        <p><b>
          <a href="/courses/<%= courses[i].fields.slug  %>">
            <%= courses[i].fields.title  %>
          </a>
        </b></p>
        <p>by <%= courses[i].fields.curator.fields.name  %></p>
        <p><%= courses[i].fields.timeToComplete  %> hours</p>
        <!-- Really flexible image API here https://www.contentful.com/developers/docs/references/images-api/#/reference/resizing-&-cropping -->
        <img src="http:<%= courses[i].fields.thumbnail.fields.file.url  %>?w=300"/>
      </li>
    <% } %>
  <% } %>
</ul>